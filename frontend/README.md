Graphical user interface for SHIELD

# install packages needed:
npm install
npm install -g http-server

# Compile on save
npm run watch

# Run server with
http-server

#server running on http://localhost:8080
